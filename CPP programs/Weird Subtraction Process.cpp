#include<bits/stdc++.h>
using namespace std;

int main(){
	long long int a, b;
	cin>>a>>b;
	bool count = true;
	while((a != 0 && b != 0) && (a >= (2*b) || b>= (2*a))){
		if(a>b){
			a= a-
		}
	}
	cout<<a<<" "<<b<<endl;
	return 0;
}
